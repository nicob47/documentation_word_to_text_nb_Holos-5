<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:H.Localization.Resources.Strings;assembly=H.Localization"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:H.Avalonia.ViewModels"
             mc:Ignorable="d" d:DesignWidth="216" d:DesignHeight="720"
             x:Class="H.Avalonia.Views.SidebarView">
    <Design.DataContext>
        <viewmodels:SidebarViewModel/>
    </Design.DataContext>
    
    <UserControl.Styles>
        <Style Selector="Button:pointerover /template/ ContentPresenter">
            <Setter Property="Background">
                <SolidColorBrush Color="{StaticResource GcdsBlue100}" Opacity="0.25"/>
            </Setter>
        </Style>
    </UserControl.Styles>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Image Grid.Row="0"
               Width="120"
               Source="/Assets/Images/holoslogo.png"
               RenderOptions.BitmapInterpolationMode="HighQuality"
               AutomationProperties.Name="{Binding [AutomationName_HolosLogo].Value, Source={StaticResource Loc}}">
        </Image>

        <Label Padding="15"
               Grid.Row="1">
            <AccessText TextWrapping="Wrap" 
                        Text="{Binding Title}"
                        TextAlignment="Center"
                        FontSize="{StaticResource GcdsFontSizeH2}"
                        FontWeight="Regular"
                        FontFamily="{StaticResource GcdsHeadingFont}"/>
        </Label>
        
        <StackPanel Grid.Row="2">
            <Button Background="Transparent"
                    Padding="10"
                    Margin="10"
                    HorizontalAlignment="Stretch"
                    Command="{Binding SwitchToClimateViewCommand}"
                    Click="Button_OnClick"
                    FontFamily="{StaticResource GcdsBodyFont}"
                    FontWeight="Regular"
                    CornerRadius="5">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Assets/Images/Icons/climate-icon.png" 
                           Width="15" 
                           Height="15"
                           RenderOptions.BitmapInterpolationMode="HighQuality"
                           AutomationProperties.AccessibilityView="Raw"/>
                    <TextBlock Text="{Binding [Label_ClimateData].Value, Source={StaticResource Loc}}"
                               Padding="10, 0, 0, 5"/>
                </StackPanel>
            </Button>
            <Button
                    Background="Transparent"
                    Padding="10"
                    Margin="10"
                    HorizontalAlignment="Stretch"
                    Cursor="Hand"
                    Command="{Binding SwitchToSoilViewCommand}"
                    Click="Button_OnClick"
                    FontFamily="{StaticResource GcdsBodyFont}"
                    FontWeight="Regular">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Assets/Images/Icons/soil-icon.png"
                           Width="15"
                           Height="15"
                           HorizontalAlignment="Left"
                           RenderOptions.BitmapInterpolationMode="HighQuality"
                           AutomationProperties.AccessibilityView="Raw"/>
                    <TextBlock Text="{Binding [Label_SoilData].Value, Source={StaticResource Loc}}"
                               Padding="10, 0, 0, 0"
                               HorizontalAlignment="Right"/>
                </StackPanel>
            </Button>
        </StackPanel>

      <Button x:Name="AboutButton"
              Grid.Row="3"
              Background="Transparent"
              Padding="10"
              Margin="10"
              HorizontalAlignment="Stretch"
              Cursor="Hand"
              Command="{Binding SwitchToAboutViewCommand}"
              Click="Button_OnClick"
              FontFamily="{StaticResource GcdsBodyFont}"
              FontWeight="Regular">
          <StackPanel Orientation="Horizontal">
              <Image Source="/Assets/Images/Icons/about-icon.png"
                     Width="15"
                     Height="15"
                     HorizontalAlignment="Left"
                     RenderOptions.BitmapInterpolationMode="HighQuality"
                     AutomationProperties.AccessibilityView="Raw"/>
              <TextBlock Text="{Binding [Label_About].Value, Source={StaticResource Loc}}"
                         Padding="10, 0, 0, 0"
                         HorizontalAlignment="Right"/>
          </StackPanel>
      </Button>


    </Grid>
</UserControl>
