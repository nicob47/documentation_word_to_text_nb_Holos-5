<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:H.Localization.Resources.Strings;assembly=H.Localization"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:avaloniaProgressRing="clr-namespace:AvaloniaProgressRing;assembly=AvaloniaProgressRing"
             xmlns:results="clr-namespace:H.Avalonia.ViewModels.Results"
             xmlns:infrastructure1="clr-namespace:H.Avalonia.Infrastructure;assembly=H.Avalonia.Infrastructure"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="H.Avalonia.Views.ResultViews.SoilResultsView">
    <Design.DataContext>
        <results:SoilResultsViewModel/>
    </Design.DataContext>
    <UserControl.Resources>
        <infrastructure1:EnumDescriptionConverter x:Key="EnumDescriptionConverter"/>
    </UserControl.Resources>
    
    <Grid RowDefinitions="Auto, Auto, Auto, *">
        <StackPanel Grid.Row="0" 
                    Name="GoBackPanel"
                    PointerPressed="GoBackPanel_OnPointerPressed"
                    Margin="10, 10, 0, 20"
                    Orientation="Horizontal"
                    HorizontalAlignment="Left"
                    Cursor="Hand">
            <PathIcon Data="{StaticResource arrow_left_regular}"/>
            <Label Content="{Binding [Label_Back].Value, Source={StaticResource Loc}}"/>
        </StackPanel>
        <avaloniaProgressRing:ProgressRing Grid.Row="1"
                               Width="30"
                               Height="30"
                               IsActive="{Binding IsProcessingData}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Top"
                               Foreground="{StaticResource GcdsSuccessBrush}"
                               Margin="0,-10,0,20"/>

        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal">
            <Button Name="ChooseSelectedSoilButton"
                    Content="{Binding [Label_ChooseSelectedSoil].Value, Source={StaticResource Loc}}"
                    IsEnabled="{Binding !IsProcessingData, Mode=TwoWay}"
                    Click="ChooseSelectedSoilButton_OnClick"
                    HorizontalAlignment="Left"
                    Background="{StaticResource GcdsBgPrimaryBrush}"
                    Margin="20, 10"
                    Cursor="Hand"/>

			<Button Name="ExportDataButton"
                    Content="{Binding [Label_ExportToCSV].Value, Source={StaticResource Loc}}"
                    IsEnabled="{Binding !IsProcessingData, Mode=TwoWay}"
                    Click="ExportDataButton_OnClick"
                    HorizontalAlignment="Right"
                    Background="{StaticResource GcdsBgPrimaryBrush}"
                    Margin="20, 10"
                    Cursor="Hand"/>

            <Button Background="Transparent"
                    Padding="10"
                    Margin="10"
                    HorizontalAlignment="Stretch"
                    Command="{Binding SwitchToClimateViewCommand}"
                    Click="Button_OnClick"
                    FontFamily="{StaticResource GcdsBodyFont}"
                    FontWeight="Regular"
                    CornerRadius="5">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Assets/Images/Icons/climate-icon.png" 
                           Width="15" 
                           Height="15"
                           RenderOptions.BitmapInterpolationMode="HighQuality"/>
                    <TextBlock Text="{Binding [Label_ClimateData].Value, Source={StaticResource Loc}}"
                               Padding="10, 0, 0, 5"/>
                </StackPanel>
            </Button>
		</StackPanel>

        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding SingleSoilResultsViewItems}"
                  Margin="20,0,20,20"
                  IsReadOnly="True"
                  SelectedItem="{Binding SelectedSingleSoilResult}"
                  IsVisible="{Binding StoragePlaceholder.ShowSingleCoordinateResults}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{Binding [Label_Latitude].Value, Source={StaticResource Loc}}"
                                    Binding="{Binding Latitude, StringFormat={}{0:0.###}}"
                                    Width="*"/>
                <DataGridTextColumn Header="{Binding [Label_Longitude].Value, Source={StaticResource Loc}}"
                                    Binding="{Binding Longitude, StringFormat={}{0:0.###}}"
                                    Width="*"/>
                <DataGridTextColumn Header="{Binding [Label_Province].Value, Source={StaticResource Loc}}"
                                    Binding="{Binding Province, Converter={StaticResource EnumDescriptionConverter}}"
                                    Width="*"/>
                <DataGridTextColumn Header="{Binding [Label_SoilGreatGroup].Value, Source={StaticResource Loc}}"
                                    Binding="{Binding SoilGreatGroup, Converter={StaticResource EnumDescriptionConverter}}"
                                    Width="Auto"/>
                <DataGridTextColumn Header="{Binding [Label_SoilTexture].Value, Source={StaticResource Loc}}"
                                    Binding="{Binding SoilTexture, Converter={StaticResource EnumDescriptionConverter}}"
                                    Width="*"/>
                <DataGridTextColumn Header="{Binding [Label_PercentClayInSoil].Value, Source={StaticResource Loc}}"
                                    Binding="{Binding PercentClayInSoil, StringFormat={}{0:0.##}}"
                                    Width="Auto"/>
                <DataGridTextColumn Header="{Binding [Label_SoilPh].Value, Source={StaticResource Loc}}"
                                    Binding="{Binding SoilPh, StringFormat={}{0:0.##}}"
                                    Width="*"/>
                <DataGridTextColumn Header="{Binding [Label_OrganicMatterSoil].Value, Source={StaticResource Loc}}"
                                    Binding="{Binding PercentOrganicMatterInSoil, StringFormat={}{0:0.##}}"
                                    Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding SoilResultsViewItems}"
                  Margin="20,0,20,20"
                  IsReadOnly="True"
                  IsVisible="{Binding StoragePlaceholder.ShowMultipleCoordinateResults}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{Binding [Label_Latitude].Value, Source={StaticResource Loc}}"
                                    Binding="{Binding Latitude, StringFormat={}{0:0.##}}"
                                    Width="Auto"/>
                <DataGridTextColumn Header="{Binding [Label_Longitude].Value, Source={StaticResource Loc}}"
                                    Binding="{Binding Longitude, StringFormat={}{0:0.##}}"
                                    Width="Auto"/>
                <DataGridTextColumn Header="{Binding [Label_Province].Value, Source={StaticResource Loc}}"
                                    Binding="{Binding Province}"
                                    Width="*"/>
                <DataGridTextColumn Header="{Binding [Label_SoilGreatGroup].Value, Source={StaticResource Loc}}"
                                    Binding="{Binding SoilGreatGroup, Converter={StaticResource EnumDescriptionConverter}}"
                                    Width="Auto"/>
                <DataGridTextColumn Header="{Binding [Label_SoilTexture].Value, Source={StaticResource Loc}}"
                                    Binding="{Binding SoilTexture, Converter={StaticResource EnumDescriptionConverter}}"
                                    Width="*"/>
                <DataGridTextColumn Header="{Binding [Label_PercentClayInSoil].Value, Source={StaticResource Loc}}"
                                    Binding="{Binding PercentClayInSoil, StringFormat={}{0:0.##}}"
                                    Width="*"/>
                <DataGridTextColumn Header="{Binding [Label_SoilPh].Value, Source={StaticResource Loc}}"
                                    Binding="{Binding SoilPh, StringFormat={}{0:0.##}}"
                                    Width="*"/>
                <DataGridTextColumn Header="{Binding [Label_OrganicMatterSoil].Value, Source={StaticResource Loc}}"
                                    Binding="{Binding PercentOrganicMatterInSoil, StringFormat={}{0:0.##}}"
                                    Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
